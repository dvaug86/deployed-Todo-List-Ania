{
  "name": "deployed-to-do-app",
  "version": "1.0.0",
  "description": "This is the first part of a project I want to build.  I'm using Ania Kubow's tutorial of building an authenticated to do app with PostGres React Node.js and Kinsta.",
  "scripts": {
    "build": "if-env TYPE=SERVER && (cd server && npm install) || (cd client && npm install)",
    "server": "node ./server/server.js",
    "client": "cd client && npm start"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "if-env": "^1.0.4"
  },
  "main": "index.js",
   "keywords": []
}
